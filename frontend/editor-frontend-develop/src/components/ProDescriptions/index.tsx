import { Descriptions, type DescriptionsProps } from 'antd'
import React from 'react'

const ProDescriptions = (props: DescriptionsProps) => {
  return <Descriptions bordered {...props} />
}

export default ProDescriptions
